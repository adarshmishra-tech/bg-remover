services:
  - type: web
    name: bg-remover-api
    runtime: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
      - key: MODEL_PATH
        value: u2net.pth
    plan: free
